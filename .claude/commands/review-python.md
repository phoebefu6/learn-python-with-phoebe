---
description: Learner-friendly review of the latest Python code in this repo
---

Review my latest Python code in this learn-python-with-phoebe repo.

Follow the "review" action in CLAUDE.md:
1. Run `git diff` (or review the most recent `projects/` folder if nothing uncommitted).
2. Correctness bugs first, then Pythonic style (naming, f-strings, comprehensions, pandas
   idioms over manual loops).
3. Add 1-2 "next thing to learn" pointers - teach, don't just flag.
4. Surface the top 3-6 points, show the fixed snippet, stay encouraging.
5. Don't rewrite everything. Apply fixes only if I ask.
6. Optional: run `ruff check .` and `black --check .` with the env's tools.

$ARGUMENTS may point at a specific file or folder - focus there if given.
